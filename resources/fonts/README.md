Roboto Regular is distributed unchanged from the Flutter 3.44.2 SDK's `bin/cache/artifacts/material_fonts/roboto-regular.ttf` artifact. The accompanying Apache License 2.0 is included in `Roboto-LICENSE.txt`.

This font provides Flutter Web's default Roboto fallback in the local preview when the app does not declare Roboto in its own font manifest.
